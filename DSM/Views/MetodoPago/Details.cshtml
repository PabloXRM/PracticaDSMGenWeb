@model DSM.Models.MetodoPagoViewModel

@{
    ViewData["Title"] = "Detalles del método de pago";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="section">
    <div class="section-header section-header-row">
        <div>
            <div class="section-kicker">Método de pago</div>
            <h2>DETALLES</h2>
        </div>
        <a class="btn ghost-btn" asp-action="Index">Volver a la lista</a>
    </div>

    <div class="detail-card">
        <div class="detail-list">
            <div class="detail-row">
                <div class="detail-label">Tipo de pago</div>
                <div class="detail-value">@Model.TipoPago</div>
            </div>

        <div class="form-actions" style="margin-top:16px;">
            <a class="btn primary-btn" asp-action="Edit" asp-route-id="@Model.Id">Editar</a>
        </div>
    </div>
</section>
